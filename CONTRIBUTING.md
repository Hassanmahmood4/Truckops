# Contributing to TruckOps

## Workflow
1. Create a branch from `main`
2. Keep commits focused and small
3. Run build checks before opening a PR

## Standards
- Prefer minimal UI changes that preserve monochrome design
- Keep API endpoints RESTful and validated
- Avoid committing secrets (`.env`, service keys)
